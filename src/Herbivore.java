public class Herbivore extends Animal{
    public Herbivore(double weight, int speedMax, double limitFood, double currentFood, int maxBreedForLocation, Location location) {
        super(weight, speedMax, limitFood, currentFood, maxBreedForLocation, location);
    }
}
